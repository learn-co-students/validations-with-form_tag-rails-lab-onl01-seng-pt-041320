<h1>New Author</h1>

<%= form_tag post_path, method: "post" do %>
<% if @post.errors.valid? %>
<div id="error_explanation">
    <h2>There were some errors</h2>
    <% @post.errors.full_messages.each do |message| %>
    <div><%= message %></div>
<% end %>

 <%if @post.errors[:title].blank? %>
  <div class="field">
<% else %>
    <div class="field_with_errors">
<% end %>
<%= label_tage "title", "Name" %>
    <% text_field_tag "title", @post.title %>
</div>

<% if @post.errors[:category].blank? %>
    <div class="field"> 
<% else %>
    <div class="field_with_errors">  
<% end %>
        <%= label_tag "category", "Email" %>
        <%= text_field_tag "category", @post.category %>
    </div>  

<% if @post.errors[:content].blank? %>
    <div class="field">
<% else %>
    <div class="field">
    <div class="fiedl_with_errors">
<% end %>
        <%= label_tag "content", "Phone Number" %>
        <%= text_field_tag "content", @post.content %>
    </div>

    <%= submit_tag "Create" %>
<% end %>
